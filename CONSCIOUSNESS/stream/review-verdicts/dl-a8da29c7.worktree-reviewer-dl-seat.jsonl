{"target_type":"feature","target_id":"FEAT-DL11","target_title":"Sequence Models (Lesson 7)","reviewer_type":"agent","reviewer_id":"dl-a8da29c7","verdict":"agent-approved","reviewed_at":"2026-09-06 03:38:07.488 gmt","note":"Independent second-pass review; both notebooks (7a theory, 7b practical) read in full. AC-1 met: RNN recurrence h_t=tanh(Wxh x_t+Whh h_prev+bh) defined and BPTT derived (direct+from-future recursive term, diag(1-h^2)Whh Jacobian, summed W_hh gradient), cell 8de26811. AC-2 met: Jacobian-product dh_T/dh_0=prod diag(1-h_t^2)Whh derived and verified empirically -- running-Jacobian-product norm plotted across 60 steps at spectral norms 0.5/1.0/1.5, composition order confirmed correct by hand-tracing jacobian_product_norms, cell aa416439. AC-3 met: LSTM forget/input/candidate/output gates each derived with role stated, additive cell-state gradient path (product of forget gates) correctly contrasted with the plain RNN's multiplicative path (toy illustration honestly labelled representative, not measured); GRU update/reset-gate simplification derived, cell 2dc5b3c0. AC-4 met: character-level RNN forward+backward implemented from scratch in NumPy (cell 6553aacb) -- hand-traced the backward pass (softmax+CE gradient, direct+future hidden-state terms, dWxh/dWhh/dWhy accumulation) and confirmed it matches the BPTT derivation exactly; verified against torch.nn.RNNCell (cell a169196e) and full autograd BPTT on 5 gradients to <1e-6 (cell 13845d12), then trained and sampled (cells 33565628, 71a3e3a4). Minor observation, not a defect: the backward function's internal np.clip(grad,-5,5) also applies during the exact-gradient verification against torch's unclipped autograd; the toy scale used keeps gradients well under that bound so the check still passes, but the clip is baked into the same function used for verification rather than isolated to the training loop. AC-5 (7b) met: nn.LSTM-based CharLSTM trained with Adam, held-out perplexity computed on a never-trained-on validation slice (85/15 split) and printed against the uniform-guess (vocab-size) baseline, cell 3e11ef22. AC-6 (7b) met: sampling swept at temperature 0.3/0.8/1.5 with correct p_i=exp(logit_i/tau)/sum formula and the coherence/diversity tradeoff characterised per value, cells 64db2ede/031e03a2. AC-7 (7b) met: teacher forcing explained and used in minibatched training (ground-truth previous character fed at every position via get_batch's shifted targets); gradient clipping applied via clip_grad_norm_ with the clip threshold deliberately set below the observed unclipped norm so engagement is measured and printed (n_clipped/n_iters), not merely present unused, cell 3e11ef22. Series consistency: 7a from-scratch NumPy RNN cell + BPTT verified to float/1e-6 precision against real references; 7b's nn.LSTM practical explicitly builds on 7a's derived LSTM gate equations. Pedagogy: story motivation before derivations throughout (weight-sharing-across-time framed against 5a's weight-sharing-across-space), a plot for every shape claim, honest about scope (7a's 64-unit RNN on one paragraph is a correctness demo not a language model; 7b's small-corpus perplexity is expected to be far from small, with 11a named as the notebook that trains at real scale). No baked cell outputs or execution counts in either notebook (checked directly against raw ipynb JSON: 10 code cells in 7a, 6 in 7b, zero with outputs/execution_count).","evidence":""}
